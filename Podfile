# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DockerBar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DockerBar
  pod 'Sparkle'
  pod 'LetsMove'
  
  target 'DockerBarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DockerBarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'DockerBarHelper' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DockerBarHelper

end
